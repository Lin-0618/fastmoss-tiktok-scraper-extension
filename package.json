{
  "name": "fastmoss-tiktok-scraper-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for exporting FastMoss video source rows and TikTok creator links.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/fastmoss-utils.js && node --check content.js && node --check popup.js"
  }
}
